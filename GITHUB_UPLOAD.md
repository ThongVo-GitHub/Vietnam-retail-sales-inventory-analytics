# GitHub Upload Checklist

## Repository name
`vietnam-retail-sales-inventory-analytics`

## Recommended description
End-to-end retail BI project: Python data cleaning, star-schema modeling, Power BI/DAX dashboarding, and independently validated KPIs on Vietnamese sales & inventory data.

## Before pushing
- Keep raw source data out of GitHub.
- Keep generated Power BI CSV exports out if they are large.
- The PBIX file is ~52 MB, below GitHub's normal 100 MB single-file limit.
- Review the dashboard screenshot one last time before making the repository public.
- Do not add API keys, credentials, tokens, or private workspace links.

## Basic Git commands

```bash
git init
git add .
git commit -m "Add Vietnam Retail Sales & Inventory Analytics portfolio project"
git branch -M main
git remote add origin <YOUR_GITHUB_REPOSITORY_URL>
git push -u origin main
```

## Suggested topics
`power-bi` `data-analytics` `business-intelligence` `python` `pandas` `dax` `star-schema` `dimensional-modeling` `data-cleaning` `retail-analytics`
