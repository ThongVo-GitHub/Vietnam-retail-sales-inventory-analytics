# SQL

SQL Server is a planned extension, not part of the current implemented pipeline.

The current project uses:

`Raw Excel -> Python/Pandas -> Star-schema CSV -> Power BI Service`

This folder is intentionally reserved for a future SQL Server staging/warehouse layer.
